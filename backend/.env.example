# Required
GEMINI_API_KEY=

# Optional — sensible defaults are used if omitted
MODEL_NAME=gemini-flash-lite-latest

# Optional — only needed when deploying (e.g. on Render). Set this to
# the deployed frontend's URL so the backend's CORS policy allows it.
# Not needed for local dev — localhost:3000 is always allowed.
FRONTEND_URL=
