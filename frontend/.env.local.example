# For local dev, point this at your locally running backend.
# When deploying (e.g. on Render), set this to the deployed backend's
# URL instead — the render.yaml Blueprint wires this up automatically
# between the two services.
NEXT_PUBLIC_API_URL=http://localhost:8000
